# Docker profile — overrides for containerized deployment
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/tokenoptimizer
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.eswarr.tokenoptimizer: INFO
